import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kedirosa.edelala',
  appName: 'edelala',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
